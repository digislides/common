name: common
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com
# author: teja <email@example.com>

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  crossplat_objectid:
  date_format: ^2.0.3
  #open_weather_api: ^1.5.5

dev_dependencies:
  test: ^1.17.10
  build_runner:

dependency_overrides:
  #open_weather_api:
  #  path: ../../dart/open_weather_map